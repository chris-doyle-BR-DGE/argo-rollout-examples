apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-active
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: rollouts-demo # Argo will dynamically append a rollouts-pod-template-hash here

---
apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-preview
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: rollouts-demo # Argo will dynamically append a rollouts-pod-template-hash here

apiVersion: v1
kind: Service
metadata:
  name: blue-green-active
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: blue-green # Argo will dynamically append a rollouts-pod-template-hash here

---
apiVersion: v1
kind: Service
metadata:
  name: blue-green-preview
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: blue-green # Argo will dynamically append a rollouts-pod-template-hash here

apiVersion: v1
kind: Service
metadata:
  name: blue-green-demo-active
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: blue-green-demo # Argo will dynamically append a rollouts-pod-template-hash here

---
apiVersion: v1
kind: Service
metadata:
  name: blue-green-demo-preview
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: blue-green-demo # Argo will dynamically append a rollouts-pod-template-hash here
